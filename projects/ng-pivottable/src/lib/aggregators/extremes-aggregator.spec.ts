import { ExtremesAggregator } from './extremes-aggregator';

describe('ExtremesAggregator', () => {
  it('should create an instance', () => {
    expect(new ExtremesAggregator()).toBeTruthy();
  });
});
