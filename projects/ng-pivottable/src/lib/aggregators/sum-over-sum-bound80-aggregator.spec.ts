import { SumOverSumBound80Aggregator } from './sum-over-sum-bound80-aggregator';

describe('SumOverSumBound80Aggregator', () => {
  it('should create an instance', () => {
    expect(new SumOverSumBound80Aggregator()).toBeTruthy();
  });
});
