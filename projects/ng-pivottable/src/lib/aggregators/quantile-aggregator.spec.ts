import { QuantileAggregator } from './quantile-aggregator';

describe('QuantileAggregator', () => {
  it('should create an instance', () => {
    expect(new QuantileAggregator()).toBeTruthy();
  });
});
