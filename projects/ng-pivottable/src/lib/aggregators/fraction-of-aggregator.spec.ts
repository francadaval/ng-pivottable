import { FractionOfAggregator } from './fraction-of-aggregator';

describe('FractionOfAggregator', () => {
  it('should create an instance', () => {
    expect(new FractionOfAggregator()).toBeTruthy();
  });
});
