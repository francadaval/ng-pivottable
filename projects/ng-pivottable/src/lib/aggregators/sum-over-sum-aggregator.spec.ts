import { SumOverSumAggregator } from './sum-over-sum-aggregator';

describe('SumOverSumAggregator', () => {
  it('should create an instance', () => {
    expect(new SumOverSumAggregator()).toBeTruthy();
  });
});
