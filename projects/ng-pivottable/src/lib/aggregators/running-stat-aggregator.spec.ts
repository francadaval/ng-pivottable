import { RunningStatAggregator } from './running-stat-aggregator';

describe('RunningStatAggregator', () => {
  it('should create an instance', () => {
    expect(new RunningStatAggregator()).toBeTruthy();
  });
});
